import BaseController from "@src/service/controllers/BaseController";

export default class NewCallController extends BaseController {
    name = 'new-call';

    actions = {
        'create': 
    }
}